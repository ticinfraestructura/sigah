# ============================================
# SIGAH - Variables de Entorno
# ============================================
# Copia este archivo como .env y configura los valores

# ============================================
# Base de Datos PostgreSQL
# ============================================
DB_USER=sigah
DB_PASSWORD=CambiaPorUnPasswordSeguro123!
DB_NAME=sigah

# ============================================
# JWT (Autenticación)
# ============================================
# Genera un secreto seguro con: openssl rand -base64 64
JWT_SECRET=CambiaEstoPorUnSecretoMuyLargoYSeguroDeAlMenos64Caracteres2024!
JWT_EXPIRES_IN=24h

# ============================================
# Aplicación
# ============================================
# Puerto interno donde se expone el frontend (Caddy hace proxy a este puerto)
APP_PORT=8080

# Base path si usas subruta (ej: /sigah para tudominio.com/sigah)
# Dejar como / si usas subdominio (ej: sigah.tudominio.com)
APP_BASE_PATH=/sigah

# Orígenes permitidos para CORS (usa HTTPS si tienes SSL con Caddy)
# Para subruta: https://saladecrisis.dosquebradas.gov.co
ALLOWED_ORIGINS=https://saladecrisis.dosquebradas.gov.co

# ============================================
# Email (Opcional - para notificaciones)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu_email@gmail.com
# SMTP_PASSWORD=tu_app_password
# SMTP_FROM=SIGAH <noreply@tudominio.com>

# ============================================
# Telegram (100% Gratuito - Recomendado)
# ============================================
# 1. Busca @BotFather en Telegram
# 2. Envía /newbot y sigue las instrucciones
# 3. Copia el token que te da aquí:
TELEGRAM_BOT_TOKEN=

# Para que un usuario reciba notificaciones:
# 1. El usuario debe enviar un mensaje al bot
# 2. Obtener su Chat ID con: curl https://api.telegram.org/bot<TOKEN>/getUpdates
# 3. Guardar el Chat ID en el campo telegramChatId del usuario
