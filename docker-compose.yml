# ============================================
# SIGAH - Docker Compose
# Sistema de Gestión de Ayudas Humanitarias
# ============================================

services:
  # ============================================
  # Base de Datos PostgreSQL
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: sigah-github-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-sigah}
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      POSTGRES_DB: ${DB_NAME:-sigah}
      TZ: America/Bogota
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - sigah-github-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-sigah} -d ${DB_NAME:-sigah}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ============================================
  # Redis (Cache y Sesiones)
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: sigah-github-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - sigah-github-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # Backend API (Node.js + Express + Prisma)
  # ============================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sigah-github-backend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      DATABASE_URL: postgresql://${DB_USER:-sigah}:${DB_PASSWORD}@postgres:5432/${DB_NAME:-sigah}
      REDIS_URL: redis://redis:6379
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-24h}
      ALLOWED_ORIGINS: "*"
      TZ: America/Bogota
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - sigah-github-network
    ports:
      - "3001:3001"

  # ============================================
  # Frontend (React + Nginx)
  # ============================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: http://localhost:3001/api
        VITE_BASE_PATH: ${APP_BASE_PATH:-/}
    container_name: sigah-github-frontend
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:80"
    depends_on:
      - backend
    networks:
      - sigah-github-network

# ============================================
# Volúmenes persistentes
# ============================================
volumes:
  postgres_data:
    name: sigah-github-postgres-data
  redis_data:
    name: sigah-github-redis-data

# ============================================
# Red interna
# ============================================
networks:
  sigah-github-network:
    name: sigah-github-network
    driver: bridge
