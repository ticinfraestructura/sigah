# ========================================
# SIGAH - Configuración de Producción
# ========================================
# Copiar a .env y configurar valores reales

# ============ BASE DE DATOS ============
# PostgreSQL (producción)
DATABASE_URL="postgresql://sigah_user:your_secure_password@localhost:5432/sigah_db?schema=public"

# Opciones de conexión
# DATABASE_URL="postgresql://user:pass@host:5432/db?connection_limit=10&pool_timeout=30"

# ============ AUTENTICACIÓN ============
# JWT Secret (mínimo 32 caracteres, usar valor aleatorio)
JWT_SECRET="your-super-secure-jwt-secret-min-32-chars-random-string-here"
JWT_EXPIRES_IN="24h"

# ============ SERVIDOR ============
PORT=3001
NODE_ENV=production

# HTTPS (opcional, si no usa proxy inverso)
# HTTP_PORT=80
# HTTPS_PORT=443
# SSL_CERT_PATH=/etc/letsencrypt/live/your-domain.com/fullchain.pem
# SSL_KEY_PATH=/etc/letsencrypt/live/your-domain.com/privkey.pem

# ============ CORS ============
# Orígenes permitidos (separados por coma)
ALLOWED_ORIGINS="https://your-domain.com,https://www.your-domain.com"

# ============ REDIS ============
# URL de conexión a Redis
REDIS_URL="redis://localhost:6379"
# REDIS_URL="redis://:password@redis-host:6379"
REDIS_PASSWORD=""
REDIS_DB=0

# ============ LOGS ============
LOG_LEVEL=info
# Opciones: error, warn, info, http, debug

# ============ BACKUP ============
# Intervalo de backup automático (horas)
BACKUP_INTERVAL_HOURS=24
# Máximo de backups a mantener
MAX_BACKUPS=30

# ============ RATE LIMITING ============
# Requests por ventana de tiempo
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200
AUTH_RATE_LIMIT_MAX=10

# ============ EMAIL (opcional) ============
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-email@domain.com
# SMTP_PASS=your-email-password
# SMTP_FROM="SIGAH <noreply@your-domain.com>"

# ============ MONITOREO (opcional) ============
# SENTRY_DSN=https://xxx@sentry.io/xxx
# NEW_RELIC_LICENSE_KEY=xxx
